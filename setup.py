from setuptools import setup

setup(name='trial_package',
      version='0.1',
      description='Trial package to test relative imports before setup',
      author='Manuel Pariente',
      url='https://github.com/mpariente/trial_package',
      )
